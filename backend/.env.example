# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=contest_db
DB_USER=postgres
DB_PASSWORD=yourpassword

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key
# Extended JWT lifetime (e.g., 30d, 12h). Defaults to 30d if unset.
JWT_EXPIRES_IN=30d

# Other Configuration
API_VERSION=v1
CORS_ORIGIN=http://localhost:3000
